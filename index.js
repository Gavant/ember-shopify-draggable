'use strict';

module.exports = {
    name: '@gavant/ember-shopify-draggable',
    options: {
        babel: {
            plugins: [require.resolve('ember-auto-import/babel-plugin')]
        }
    }
};
